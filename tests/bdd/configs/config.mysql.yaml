server:
  host: "0.0.0.0"
  port: 8081
storage:
  type: mysql
  mysql:
    host: mysql
    port: 3306
    database: schemaregistry
    user: schemaregistry
    password: schemaregistry
compatibility:
  default_level: BACKWARD
logging:
  level: info
  format: json
