server:
  host: "0.0.0.0"
  port: 8081
  docs_enabled: true
storage:
  type: cassandra
  cassandra:
    hosts:
      - cassandra
    port: 9042
    keyspace: schemaregistry
    consistency: ONE
    migrate: true
compatibility:
  default_level: BACKWARD
logging:
  level: info
  format: json
